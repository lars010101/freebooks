# freeBooks API — environment variables
# Copy to .env and fill in values

# Path to the DuckDB database file
DB_PATH=../data/freebooks.duckdb

# Port and bind address — localhost only, no external exposure
PORT=3000
HOST=127.0.0.1

# API secret for simple bearer-token auth (optional)
API_SECRET=change-me-before-production
