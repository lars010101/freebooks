# Data — never commit financial data
data/
*.duckdb
*.duckdb.wal

# Node
node_modules/
.env
.env.*
!.env.example

# MCP server dependency install (managed by its own mcp/package.json)
mcp/node_modules/

# Evidence build output
reports/.evidence/
reports/build/

# OS
.DS_Store
