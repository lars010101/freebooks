{
  "name": "skuld-functions",
  "version": "0.1.0",
  "description": "Skuld — Cloud Functions logic layer for financial workbook",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "lint": "eslint src/ tests/",
    "deploy": "gcloud functions deploy skuld --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point=handler --source=."
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.0.0",
    "@google-cloud/functions-framework": "^3.0.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "license": "AGPL-3.0-or-later"
}
