# freebooks — IA Restructure Spec 3: Report tab-ification, Books → Journal, Integrity Check relocation

**Date:** 2026-08-30 · **Status:** IMPLEMENTED (2026-08-30)
**Scope:** (1) **Prerequisite** — replace the iframe-per-selection loading mechanism shared by Statements, Books, and Payables' Aging/Control tabs with a fetch-and-cache fragment loader **for every report type except `voucher-register` (Transactions), `journal` (Line items), and `ap-aging`, which keep an isolated, auto-resizing `<iframe>`** — see the 2026-08-30 changelog entries: those three have no server-rendered fallback (content is entirely populated by an embedded `<script>` calling `FB.list.create()`, unsafe to execute in the shared host-page scope). `gl`, `tb`, `ap-control`, and all four Statements reports are genuinely server-rendered and stay on the fragment path. (2) Statements' report-type dropdown becomes a visible tab strip (mechanism only, no content change); MoM/YoY step controls stay Statements-exclusive. (3) Books is renamed **Journal**, route path changes `/:company/books` → `/:company/journal` with no redirect (gKey `b`→`j`), its tabs are relabeled, Integrity is removed from it, and it gains the **SIE export** affordance (moved from Books). (4) Accounting keeps its name and its four existing tabs, and gains a fifth: **Integrity**, rendered inline (not via iframe), with a tab-dot indicator on failure. (5) Route-level fallout from the rename — an existing `/:company/journal` redirect handler collides with the new route and must be deleted; a pre-existing dead `/:company/bank` redirect and its unrouted dead-code referrer are flagged for cleanup; `fb-core.js`'s two hardcoded `/books` string checks must be updated in the same change.
**Companions:** `ia-restructure-2-spec.md` (2026-08-27, immediate predecessor — this spec supersedes its §2.1/§3.2/§3.5 entries for Books and Accounting **and its §3.3 claim that SIE export surfaces exclusively through Fiscal → Filings** — after discussion, SIE is treated as an anytime data export tied to the ledger, not a filing artifact, so it stays on the renamed Journal page instead (§3.2). IA2's Filings-exclusivity rule for VAT return/INK2/Annual Report is untouched — this reversal is SIE-specific.), `report-registry.js`, `reports-hub.js`, `payables.js` (shares the iframe pattern §1 fixes), `fb-core.js` (hardcoded `/books` route-string checks, §2.3)
**Consumers:** `api/src/nav-registry.js`, `api/src/report-registry.js`, `api/src/pages/reports-hub.js`, `api/src/pages/payables.js` (inherits the loading-mechanism fix), `api/src/pages/accounting.js`, `api/src/reports.js` (existing `/:company/journal` and `/:company/bank` route handlers, §2.1–2.2), `api/public/fb-core.js` (route-string checks, §2.3), `reports/render.js` (`buildIntegrity` — read, not modified), `db/macros.sql` (`integrity`/`integrity_extended` — read, not modified)

---

## 0. Explicitly out of scope

1. **The periodic/background Integrity Check nag.** Discussed and deliberately deferred — only the tab-local dot indicator ships in this pass. A scheduled check (piggybacking on `agent-loop.js`'s tick, or a new job) plus an Inbox item is a separate, later spec.
2. **The check logic itself.** `integrity()`/`integrity_extended()` (`db/macros.sql`) and `buildIntegrity()` (`reports/render.js`) are unchanged — this spec is about where results surface, not what's evaluated or how the current-year-unclosed-profit adjustment works (already handled, see `review-roadmap.md` discussion 2026-08-29).
3. **Fiscal and Payables page contents.** Unaffected except that Payables' Aging/Control tabs inherit the §1 loading-mechanism fix, since they already share the exact iframe pattern this spec removes elsewhere.
4. **Renaming "Accounting."** Explicitly kept as-is this round (open item from prior discussion, resolved: no rename).

---

## 1. Prerequisite — kill the iframe-per-tab loading pattern

### 1.1 Problem, confirmed against source

Three surfaces currently load report content into a `<iframe src="about:blank">` whose `src` is rewritten to `report?type=X` on selection — a full nested-document navigation, not a fragment fetch:

| Surface | File | Mechanism |
|---|---|---|
| Payables → Aging/Control tabs | `api/src/pages/payables.js:415-422,466-471` (`loadReportFrame`) | `#aging-frame`/`#control-frame` iframe `src` set per tab switch |
| Statements / Books report picker | `api/src/pages/reports-hub.js:76` (`#report-frame`) | Single iframe, `src` set per `<select>` change |

Every switch pays full page-load cost: a new HTTP round trip, a full server-side render (query → HTML string via `reports/render.js`), and a full re-parse of that document's own embedded `<head>`/`commonStyle()` CSS — with no client-side caching, so revisiting a tab you already loaded this session re-does all of it. This is the "I don't like that feeling" reported on Payables (2026-08-29 session), and it is about to be multiplied: Statements moving to a 4-tab strip and Journal (§3.2) keeping 4 report tabs means up to 8 more of these full-navigation loads across two pages that don't have it today, on top of Payables' existing 2.

### 1.2 Fix direction

Replace iframe navigation with `fetch()` of the report fragment, injected into a plain `<div>` in the host page (same DOM, same stylesheet — no nested document, no duplicate `<head>` parse). Cache the fetched fragment per tab for the session (in-memory JS object keyed by report type + the query params that affect it — period/start/end/asOf — is sufficient; no need for `sessionStorage` durability here, this is a perf cache, not navigation state). A tab switch back to an already-fetched combination renders from cache instantly; a param change (period, date range) invalidates that tab's cache entry.

This requires `reports/render.js`'s report builders to be reachable as a fragment response (table HTML without the surrounding `<!DOCTYPE html>`/`<head>`/`commonStyle()` shell) — check whether `report?type=X` already supports a fragment mode (e.g. an `&embed=1` param) before assuming new server work; if it doesn't, that's the one required server-side change here, otherwise this is client-side only.

**This is a blocking prerequisite for §3.1 and §3.2** — do not build the Statements tab strip or the Journal page's tab strip on the current iframe mechanism; that would ship the exact problem being fixed, at greater multiplicity.

---

## 2. Route registry changes (`api/src/nav-registry.js`)

| Key | Route | Label | gKey | Change |
|---|---|---|---|---|
| `statements` | `/:company/statements` | Statements | `t` | unchanged route/label/gKey — picker becomes a tab strip (§3.1), content unchanged |
| `journal` | **`/:company/journal`** *(renamed from `/:company/books`)* | **Journal** *(renamed from Books)* | **`j`** *(was `b`)* | Tabs relabeled, Integrity removed (§3.2) |
| `accounting` | `/:company/accounting` | Accounting | `a` | unchanged route/label/gKey — gains a 5th tab (§3.3) |

**Route path change, confirmed.** `/:company/books` is deleted, not redirected — per IA2's clean-cutover doctrine (§2.3 of that spec: single-user install, no bookmarks to preserve). `/:company/journal` is the sole route from the moment this ships; any internal link still pointing at `/books` (nav-registry consumers, `action-catalog.js` palette entries if any reference it, `common.js`'s `topBarContext` key map per IA2 §2.6's precedent) must be updated in the same change, not left to 404.

### 2.1 CRITICAL — an existing `/:company/journal` route already exists and must be deleted

`api/src/reports.js:261-265` currently registers `/:company/journal` as a 302 redirect to `/books?t=voucher-register`, left over from an earlier dissolution of a standalone Journal page into the Books hub:

```js
app.get('/:company/journal', function(req, res) {
  res.redirect(302, '/' + req.params.company + '/books?t=voucher-register');
});
```

This handler must be **deleted and replaced** with the real page handler for the renamed Journal page — not left alongside it (Express would just use whichever is registered first; leaving both is a guaranteed source of "why doesn't my page load" confusion, not a silent conflict).

`/:company/journal/voucher` (`reports.js:265`, `handleJournalVoucherPage`) sits immediately after it. **Correction to an earlier draft of this finding:** Express's default route matching is exact-segment (`path-to-regexp` compiles `/:company/journal` to match only a two-segment path), so it will **not** match the three-segment `/:company/journal/voucher` regardless of registration order — there is no actual swallow risk here with plain string routes as written today. Worth keeping the general habit of registering more specific paths first as a defensive precaution (in case the new page handler ever grows a wildcard/catch-all for its own tab sub-paths), but it is not a live ordering bug as things stand.

### 2.2 `/:company/bank` — confirmed dead code, recommend deletion rather than repointing

`reports.js:276-278` redirects `/:company/bank` to `/books`, which would 404 once `/books` is deleted. Traced further: nothing reachable actually links to `/bank` today. The only two live references are hardcoded `<a href="/${co.company_id}/bank...">` cards in `api/src/pages/company.js:147,152` — but `handleCompanyPage` (that file's export) is **imported in `reports.js:18` and never registered on any route**, i.e. it's already unreachable dead code left over from Dashboard being dropped (roadmap §0y, 2026-08-03: "Inbox is now the root route"). One of that same dead page's other cards links to `/settings?tab=periods`, also a dead route since IA2 slimmed Settings and explicitly deleted that redirect handler — confirming the whole block predates both migrations and was never cleaned up.

**Recommendation:** delete `reports.js:276-278`'s `/bank` redirect and the dead `company.js` dashboard-card block outright (standing rule 6 — dead code deleted, not commented/repointed). There is no live target to repoint it to; `bank.match`/`bank.reconcile.*` server actions remain in `api/src/bank.js` for the agent pipeline and are unaffected either way.

### 2.3 `fb-core.js` — hardcoded `/books` path-string checks

`api/public/fb-core.js:1137` (`_pageLabelFor()`) and `:1153` (`showTargets()`) string-match on the route path rather than reading the registry key:

```js
if (route.indexOf('/books') === 0) return 'Books';           // :1137
if (meta.route.indexOf('/books') === 0) return;               // :1153, excludes Books from something (needs checking what)
```

Changing `nav-registry.js` does not auto-fix these — both need `/books` → `/journal` and the returned label `'Books'` → `'Journal'` as an explicit, separate edit in the same change.

**`g b` is freed** by this rename and currently has no reservation — same status as `g m`/`g v`/`g d` per IA2's g-key slate comment. Not claimed by this spec.

---

## 3. Section definitions

### 3.1 Statements (`/:company/statements`) — dropdown → tabs

Mechanism-only change, per §1's prerequisite. Same four entries, same order, now rendered as a visible tab strip (`h`/`l` cycles, matching Payables/Accounting's existing tab pattern) instead of a `<select>`:

`Profit & Loss` · `Balance Sheet` · `Cash Flow` · `Statement of Equity`

No relabeling, no reordering, no `REPORT_REGISTRY` id changes.

**MoM/YoY step controls stay on this page's tabs only**, driven per-tab by each entry's existing `multiperiod` flag (`pl`/`bs`/`cf` = true, `sce` = false — unchanged data, already correct in `report-registry.js`). This is a page-level guarantee as much as a per-report one: the Journal page (§3.2) never renders MoM/YoY chrome at all, on any of its four tabs — worth stating explicitly now that both pages share the same tab-strip mechanism, so an implementer doesn't assume symmetric chrome across the two pages by default.

### 3.2 Journal (renamed from Books) — dropdown → tabs, relabeled, Integrity Check removed

| New tab label | `REPORT_REGISTRY` id | Old Books label |
|---|---|---|
| Transactions | `voucher-register` | Transaction Register |
| Line items | `journal` | Journal Line Listing |
| Trial Balance | `tb` | Trial Balance *(unchanged)* |
| General Ledger | `gl` | General Ledger *(unchanged)* |

Only the label strings change for the first two; `REPORT_REGISTRY` ids, routes, and underlying builders (`buildVoucherRegister`, `buildJournal`) are untouched.

**No MoM/YoY chrome on this page** — all four tabs are already `multiperiod: false` in `report-registry.js`; this is confirmed unchanged data, restated here as an explicit page-level contract (§3.1).

**`integrity` is removed from this page's tab list** and from `reportsByPage('books')`'s effective output for Journal — it relocates to Accounting (§3.3). `report-registry.js`'s `REPORT_REGISTRY` entry for `integrity` needs its `page` field changed away from `'books'` — see §3.3's open question on whether it stays in `REPORT_REGISTRY` at all.

**SIE export moves here, stays here — not Fiscal → Filings.** `reports-hub.js:30`'s `sieExportEnabled` gate (`pageKey === 'books'`) needs its literal updated to `'journal'` — a required change regardless of the placement debate. But the placement itself was actively re-litigated: `ia-restructure-2-spec.md` §3.3 states regulatory documents including SIE 4 export should surface "exclusively through the Filings tab" — never implemented in code (the SIE button has only ever lived on the Books hub) and, on reconsideration, wrong for SIE specifically. **Decision (2026-08-30, after pushback):** SIE is an anytime data export of the ledger (IB/UB/RES/VER — the same voucher/balance data as this page's other three tabs), not a filing tied to a due date or a submission workflow the way VAT return/INK2/Annual Report are — those stay Filings-exclusive per IA2, unchanged. SIE belongs with the ledger content it exports, i.e. **Journal**, not Accounting (nothing on Accounting's tabs is transactional data) and not Fiscal → Filings (no filing-workflow fit). Keep it as page-level chrome, visible regardless of which of the four tabs is active — same behavior as today, just re-gated on `'journal'` instead of `'books'`.

### 3.3 Accounting (`/:company/accounting`) — gains an Integrity tab

**Tabs (default: Chart of Accounts):** Chart of Accounts · Tax Codes · Journals · Cost/Profit Centers · **Integrity**

Tab label is **"Integrity"**, not "Integrity Check" — shorter, and the tab strip context (a page of ledger-structure tabs) already implies "check." This is the tab label only; whether `REPORT_REGISTRY`'s `integrity` entry's own `label` field ("Integrity Check", used elsewhere e.g. command palette) also changes to match is a separate, smaller call — recommend renaming it too for consistency rather than carrying two names for the same check, but not fixing that decision here.

Rationale (from discussion): Integrity validates exactly this page's contents (COA structure, journal balance) and is fixed by the same person doing COA/Journals work; it's quiet by default (no dot) unless a check fails.

**Rendering mechanism — deliberately not an iframe.** `ia-restructure-2-spec.md` §5.2 already establishes Accounting's contract as "FB.list on every tab" — no iframe exists on this page today, and embedding the Integrity report via iframe would both break that established pattern and reintroduce the exact loading problem §1 removes elsewhere. Integrity's output is a small, static (no period picker beyond the page-level date context), read-only result set — well suited to a direct `fetch()` of the check results rendered into a plain table by the Accounting page's own script, consistent with §1's fragment-fetch direction. **This is not FB.list** (there's nothing to edit/add/delete — it's a report, not a register), so it's a third rendering mode on this page (FB.list ×4, plain fetched table ×1), which should be stated explicitly in the page's implementation, not left implicit.

**Open question, not resolved here:** does `integrity` stay in `REPORT_REGISTRY` (with `page` repointed, e.g. to a new value or `null` if the registry's `page` field is meant to only describe report-hub pages) so `report?type=integrity` remains independently reachable (e.g. for the command bar, or a future non-Accounting consumer), or does it come out of the registry entirely and become bespoke Accounting-page code? Recommend keeping it in `REPORT_REGISTRY` — the registry is the single declarative report list (per its own header comment) and other consumers (command palette, export adapters) may still want `integrity` addressable by id — but flagging since IA2's registry `page` field currently only has two values (`'statements' | 'books'`) and a third page consuming a registry entry outside the report-hub shell is new.

**Indicator — dot only, no periodic nag (§0 item 1).** Reuse the existing tab-dot mechanic already on Accounting's other four tabs (`#tab-dot-coa` etc., `accounting.js:53-56`) to signal a failing check. **Flag, not resolved:** on the other four tabs, that dot means "you have unsaved edits in this tab" (`markDirty`/`resetDirty`, `accounting.js:101-108`). Reusing the same visual for "this check is failing" is a different semantic riding the same mechanism — a dot on Integrity doesn't mean "unsaved," it means "attention needed." Cheap to ship as-is (same CSS, same DOM pattern, no new component), but if it causes confusion in practice, the two meanings should get visually distinct treatments (e.g. a status-colored dot vs. the existing amber dirty-dot) — not committing to that now, just flagging so it isn't a surprise later.

---

## 4. What does not change

- Fiscal, Payables, Settings, Exchange Rates, Inbox, Documents, Calendar — untouched by this spec (Payables inherits §1's mechanism fix only). Fiscal → Filings specifically does **not** gain SIE export (§3.2) — VAT return/INK2/Annual Report remain its only artifacts, per IA2, unchanged.
- The check logic, tolerances, and current-year-profit adjustment inside `integrity()`/`integrity_extended()`/`buildIntegrity()`.
- `REPORT_CATEGORIES`/`category` optgroup machinery in `report-registry.js` — already noted in that file's own comment as "retained for backward compat but no longer drives optgroups." This spec doesn't touch it, but removing `integrity`'s Books placement makes the Books/Journal category grouping even more vestigial than it already was; a future cleanup pass could drop `REPORT_CATEGORIES` outright once nothing reads it, but that's not this spec's job.

---

## 5. Open questions / deferred (parking lot)

1. **Periodic Integrity nag** — deferred per §0 item 1. Needs its own spec: a scheduled evaluator + Inbox item, modeled on the existing `period_unclosed` Class B pattern.
2. **Dot semantic collision** (dirty-tab vs. check-failed) — flagged in §3.3, not resolved. Ship as one mechanism for now; revisit if it's confusing in practice.
3. **Whether `integrity` stays in `REPORT_REGISTRY`** — flagged in §3.3, leaning toward "stays, with `page` field's contract loosened," not decided.
4. **Whether `REPORT_REGISTRY`'s `integrity.label` ("Integrity Check") is also renamed to "Integrity"** for consistency with the new tab label — recommended in §3.3, not fixed as a requirement.
5. **Whether `report?type=X` already supports a fragment/embed response mode** — needs checking against `reports/render.js` before scoping §1 as client-only vs. requiring a server change.

---

## 6. Follow-up: report chrome cleanup — header, unified download, CSV correctness (2026-08-30, IMPLEMENTED)

Ratified in a "talk, don't act" design discussion the same day as the rest of this spec's work. Three related problems, one body of work:

### 6.1 Problem statement

1. **Every report repeats chrome the page already shows.** Each report's own `.header` div (Company / Report Title / Period) duplicates the topbar company name, the active tab's own label, and the global period selector — confirmed redundant on screen, but not decorative: the PDF export path (`fbExportPDF()`, `window.open(buildReportUrl())`) reuses this exact HTML raw, so the header is what makes a downloaded PDF a self-contained document once it's outside the app.
2. **Download UI is per-page, inconsistent, and in the wrong place.** Today only Statements/Journal have a download control at all (`reports-hub.js`'s `tb-controls-row`, positioned below the tab strip, not in the actual global topbar which already has a row of `tb-icon-btn` buttons for Chat/New/Notifications/Theme/Help — `common.js` lines ~157-162). Payables' Aging/Control tabs have no download affordance whatsoever — confirmed, not assumed.
3. **CSV export is broken for the three iframe-based reports.** `fbExportCSV()` scrapes `<table>` cells from the host page's own DOM (`document.querySelectorAll('table')` / `body.querySelectorAll('table')`). voucher-register, Line items, and GL render inside an `<iframe>` — `document.querySelectorAll` cannot reach across that boundary, so CSV export on those three currently finds nothing. Separately (this spec's own earlier work, §"Atomic grouping"/"Two follow-up refinements"), Line items and GL blank repeated Doc No/Journal/Date/Account values for on-screen readability — a display-only convenience with no place in a data-interchange format. Both problems have the same fix: CSV must read each report's underlying row data (`JL_ROWS`/`GL_ROWS`/`VR_ROWS` — already in memory, unblanked, exactly why the blanking is display-only and not a data mutation), never scrape the rendered DOM.

### 6.2 Header — screen-hidden, print-visible, not deleted

The header stays in every report's server-rendered HTML (both fragment and iframe reports) so PDF export keeps its context. It's hidden for the **embedded/screen** view only, via a print-scoped visibility rule (`@media print` restoring it, or an explicit class the fragment/iframe wrapper toggles) — not removed from the markup, since PDF reuses the same HTML. Reports should visually begin at the column headers, immediately below the page's own tab strip, on screen.

### 6.3 Unified topbar download icon

One `tb-icon-btn`-styled icon added to `common.js`'s existing topbar icon row (next to Notifications/Theme/Help — same file, same convention, present on every page automatically). Always visible, never hidden — its **dropdown contents** adapt to context, not its presence:

- **SIE** — shown whenever the active company's jurisdiction pack declares `integrations.sie.export` (existing `isSieExportEnabled`/`packIntegration` check, unchanged), independent of which page or tab is active. SIE is a company+period export (IB/UB/RES/VER for the selected date range via the always-available global period selector), never "the current tab" — §6.5 below on why it stays type-4-only.
- **CSV** / **PDF** — shown only when the *active page/tab* has exportable tabular content. A page/tab with nothing to export (Settings, Accounting's non-Integrity tabs, Documents) shows SIE alone if available, or nothing.

**Contract a page uses to tell the icon what's available**, kept deliberately simple — a couple of `window`-scoped hooks a page's own script (re)sets whenever its active tab changes, not a formal register/subscribe API (three page families need this, not fifteen; a pub-sub layer would be solving a bigger problem than exists):

```js
window.__fbDownloadCsv = function () { /* returns rows, or CSV text */ } || null;
window.__fbDownloadPdfUrl = 'https://…' || null;   // or null when nothing to print
```

The topbar icon (defined once, in `common.js`, alongside the existing theme/notifications wiring) reads these two hooks at click time to decide what to render in its dropdown, and calls whichever the user picks. Each page family (`reports-hub.js`, `payables.js`, `accounting.js`) sets/clears them on tab switch — three call sites, not a new subsystem.

**Cross-iframe consequence:** for the three iframe-based reports, `window.__fbDownloadCsv` in the HOST page can't reach into the iframe's own `JL_ROWS`/`GL_ROWS`/`VR_ROWS` directly (different `window`, different global scope) — it needs to either read them via `frame.contentWindow.JL_ROWS` (same-origin, already relied on elsewhere in this spec — e.g. `renderIframe`'s ResizeObserver already reaches into `frame.contentWindow.document`) or have the iframe's own script `postMessage` its current row array up on load/tab-change. Same-origin direct access is simpler and consistent with the existing pattern; recommend that over `postMessage` unless a concrete reason surfaces.

### 6.4 CSV correctness

CSV export switches from DOM-scraping to reading each report's own row-data array (`JL_ROWS`/`GL_ROWS`/`VR_ROWS` for the three iframe reports; the seven fragment-rendered reports have no such array today and can keep DOM-scraping — they have no blanking concern and no iframe boundary to cross, so nothing forces a change there). No settings/toggle for fill-vs-blank — CSV always emits the full unblanked value on every row; blanking is screen-only. PDF keeps the on-screen blanked appearance (it's a human-read, print-like artifact, same readability rationale that motivated blanking in the first place).

### 6.5 SIE type scope — no change

Confirmed against the SIE-gruppen spec: types 1–4 are a strict superset chain (1 = annual balances only, 2 = + period balances, 3 = + cost-center/object detail, 4 = + full transactions). `renderSie()` emits type 4 only, which already contains everything types 1–3 would. No plan to add 1–3 support — the one real consumer (Gredor) specifically requires type 4 (the 8999 `#RES` line), and nothing in the current workflow needs a lesser type. Revisit only if a concrete second consumer requires otherwise.

### 6.6 Open questions / not yet decided

1. **Print-visibility mechanism for the header** — `@media print` vs. an explicit screen/print class toggle. Leaning toward `@media print` (simplest, no JS needed) but not fixed.
2. **Same-origin `frame.contentWindow` access vs. `postMessage`** for the CSV hook reaching into iframe-based reports — leaning toward direct access (§6.3), not fixed.
3. **Exact dropdown copy/ordering** when only SIE is available (1 item) vs. SIE+CSV+PDF (3 items) — not designed yet, implementation-level detail.

---

## 7. Changelog

| Date | Change |
|------|--------|
| 2026-08-30 | Spec drafted per design-review discussion (2026-08-29–30 session): iframe-per-tab loading identified as a shared perf problem across Payables/Statements/Books and set as a blocking prerequisite (§1); Statements' dropdown becomes a tab strip (§3.1); Books renamed Journal, tabs relabeled, Integrity removed (§3.2); Accounting keeps its name and gains an Integrity tab rendered inline via fetch (not iframe), with a reused dirty-tab-dot indicator and no periodic nag for now (§3.3). Status: PROPOSED, pending ratification. |
| 2026-08-30 | Round 2 applied: (1) MoM/YoY step controls confirmed Statements-exclusive — stated as an explicit page-level contract on both Statements (§3.1) and Journal (§3.2), though the underlying `multiperiod` data was already correct and unchanged; (2) Accounting's new tab renamed **"Integrity"** (was "Integrity Check") throughout, with a new open question (§5 item 4) on whether `REPORT_REGISTRY`'s own `integrity.label` follows suit; (3) the route-path question is resolved, not deferred — `/:company/books` is deleted outright and replaced by `/:company/journal`, no redirect, per IA2's clean-cutover doctrine; removed from the open-questions parking lot. |
| 2026-08-30 | Round 3 applied, external code review against actual source (findings verified, not taken on faith): (1) **CRITICAL** — an existing `/:company/journal` 302-redirect handler (`reports.js:261-265`, left over from an earlier Journal→Books dissolution) collides with the new route and must be deleted, not left alongside the new page handler (§2.1); corrected the review's route-ordering claim — Express's exact-segment matching means `/:company/journal/voucher` was never actually at swallow risk regardless of registration order; (2) traced `/:company/bank`'s dead redirect (`reports.js:276-278`) to its only live referrer, `company.js:147,152` — itself unreachable dead code (`handleCompanyPage` is imported but never routed, orphaned since Dashboard was dropped 2026-08-03) — recommend deleting both rather than repointing either (§2.2); (3) `fb-core.js:1137,1153`'s hardcoded `/books` string checks added as a required, separate edit — changing `nav-registry.js` does not auto-fix them (§2.3); (4) **SIE export placement re-litigated and reversed:** pushback rejected the Filings-exclusive framing (SIE is an anytime ledger export, not a filing workflow) — decision is **Journal**, not Accounting or Fiscal → Filings; this explicitly supersedes IA2 §3.3's SIE-specific claim (its VAT return/INK2/Annual Report Filings-exclusivity rule is untouched) (§3.2, header Companions line). |
| 2026-08-30 | **Implemented.** All sections shipped: §1 fragment-fetch+cache loader (no server change needed — `report?type=X` already returns full HTML, the client parses out `.page` via `DOMParser`) replaces the iframe on Statements, Journal, and Payables' Aging/Control; §2/§2.1–2.3 route rename + dead-code deletion (the colliding `/:company/journal` redirect, `/:company/bank` redirect, and the entire dead `pages/company.js` — not just its `/bank` cards, the whole file was unrouted — plus the already-dead `pages/journal.js`, discovered as a naming collision with the new route while implementing, deleted too); §3.1/§3.2 tab strips; §3.3 Accounting's Integrity tab (fetch-rendered, dot indicator uses a distinct red vs. the dirty-dot's amber — a lightweight partial answer to the §5 item 2 semantic-collision flag, not a full resolution). Deviations from the letter of the spec, both toward *less* than what was described: (a) no h/l tab-cycling — no such keyboard precedent actually exists on Payables/Accounting's tabs (mouse-only today), so none was added, consistent with the frozen-verb-surface doctrine; (b) the per-tab cache is keyed directly by request URL rather than a separate type+params cache-key scheme — simpler, same effect. Additional consumers found and fixed during implementation, beyond the spec's enumerated list: `fb-command.js`'s `:report` route builder (+ a new `integrity` → `/accounting?tab=integrity` special case), `journal-voucher.js`'s Quit-button return URL, `common.js`'s `topBarContext` map, and `api/test/command-parser.test.js`'s route assertion. Full API test suite 211/211, jurisdiction-pack linter and mcp-smoke clean. **Not fixed:** three tracked `pw-*.mjs` Playwright probe scripts (`pw-k3d.mjs`, `pw-k3e.mjs`, `pw-reports-cells.mjs`) reference the old iframe/`/books` mechanics — none are wired into `npm test`/`test:e2e`, so nothing is gated on them, but they'll need updating before their next manual use. |
| 2026-08-30 | **Post-implementation bug fix, live-verified with Playwright against a real running instance.** All fetched reports showed "Report returned no content" on first live test. Root cause: `common.js` globally monkey-patches `window.fetch` for every `/api/*` URL (the P0-2 error-envelope normalizer) — it unconditionally calls `r.json()` and, on parse failure, falls back to `null`, then serves `text()` as `JSON.stringify(null)` = the literal 4-character string `"null"`. `/api/:company/report?type=X` returns HTML (or a binary SIE attachment), never that JSON envelope, so every fragment fetch this spec introduced (reports-hub.js, accounting.js's Integrity tab, payables.js's Aging/Control tabs) was silently getting `"null"` instead of the real body — a pre-existing app-wide interceptor that the old iframe mechanism never went through (iframes navigate, they don't call `fetch()`). **Fixed:** `common.js`'s wrapper now passthroughs (`_fbOrigFetch`, unwrapped) any URL containing `/report?` — precise enough not to catch `/api/:company/reports/default-period` (no `?` immediately after `report`), which does return JSON and still wants normalization. Re-verified live: Statements, Journal, and Payables Aging all render real report content. **Found, not fixed (separate, pre-existing, out of this spec's scope):** Accounting's Integrity tab surfaces a genuine server-side DuckDB error for company `test23` specifically (confirmed absent for `mdab_se`) — `Invalid Input Error: More than one row returned by a subquery used as an expression` — from `buildIntegrity()`/`integrity()`/`integrity_extended()` (`db/macros.sql`, `reports/render.js`), code this spec explicitly kept read-only. Latent bug, company-specific, unmasked by giving Integrity a reachable UI for the first time in a while — needs its own investigation into which subquery lacks a `LIMIT 1` against that company's data. |
| 2026-08-30 | **Second post-implementation bug, live-verified: "Journal → Line items stuck at Loading…" (company `mdab_se`).** Root cause, found via a live debug-instrumented server restart (not guessed): `buildJournal()` in `reports/render.js` doesn't server-render its table at all — it ships an empty shell (`<tbody id="table-body"><tr><td>Loading…</td></tr></tbody>`) plus a `<script>` that fetches real rows client-side via `action: 'journal.list'`. Setting `innerHTML` never executes embedded `<script>` tags (HTML5 spec), so that script never ran and the placeholder was permanent. First fix attempt (recreating `<script>` elements to force execution) surfaced a **second, more serious problem**: that script — and `buildAPAging`'s — call `FB.list.create(...)`, written assuming an isolated iframe with its own independent `FB` framework instance; executing it in the shared host page broke the host's own `FB.period` state app-wide (confirmed live: every report on the page started failing with "Select a period first"). **Final fix, more conservative than the original spec's blanket fetch-and-cache:** only `journal` (Journal Line Listing) — the sole report with no server-rendered fallback — keeps the old isolated `<iframe>` mechanism; every other report type (`voucher-register`, `tb`, `gl`, `pl`, `bs`, `cf`, `sce`, `ap-aging`, `ap-control`, `integrity`) is fully server-rendered and safe to fragment-inject without executing its script — their embedded scripts (where present: GL, Transactions, AP Aging) simply don't run, same inert-interactivity tradeoff as the immediate post-implementation state, not a new regression. Live-reverified end-to-end across Journal (all 4 tabs), Statements (all 4), Payables Aging/Control, and Accounting Integrity — all render real content, zero page errors. Full API suite re-run clean: 211/211. |
| 2026-08-30 | **Third and fourth post-implementation bugs, both from the same under-checked assumption in the second fix above.** (1) *"Journal → Line items: scroll bar on the left, bottom cut off."* The `journal` iframe (from the fix above) had a fixed `min-height:600px` — content taller than that scrolled *inside* the iframe instead of the page just growing, and the mismatch between the iframe's own scrollbar and the outer flex container's `overflow:auto` produced the stray/misplaced scrollbar. Fixed: iframe now auto-resizes to `contentWindow.document.documentElement.scrollHeight` on `onload`, no internal scroll, the report prints downward inside the host page's own scroll as expected. (2) *"Journal → Transactions: nothing listed, even though Line items shows items exist."* My "safe to fragment-inject, only interactivity goes inert" claim for `voucher-register` and `ap-aging` was **wrong, verified by direct inspection this time, not assumed**: both ship an *empty* `<tbody></tbody>` populated entirely by the same `FB.list.create().load()` pattern as `journal` — no server-rendered fallback at all, so fragment-injecting them (never executing their script) left the table completely empty, not just non-interactive. The earlier regression sweep's "OK" checks only tested for the *absence* of the literal word "Loading" and a length threshold — both false-negative for a silently-empty-but-chrome-present shell. Fixed: `voucher-register` (reports-hub.js) and `ap-aging` (payables.js) both moved into the iframe set alongside `journal`, with the same auto-resize. `gl`, `tb`, `ap-control`, and all four Statements reports remain fragment-injected — verified again by curling each type directly against `mdab_se` and checking for real `<tr>` rows in the raw response, not inferring from byte counts. Re-verified live (iframe found + real rows present for voucher-register and journal; outer container's `scrollWidth === clientWidth` and `scrollHeight === clientHeight`, i.e. no forced scrollbar). Full API suite re-run clean: 211/211. Aging's empty table for `mdab_se` specifically was checked and is real — `bill.aging` fires and returns cleanly with no open payables for that company, not a loading failure. |
| 2026-08-30 | **Journal Line Listing redesigned per follow-up request** (`buildJournal`, `reports/render.js`): rewritten from a bespoke fetch/sort/filter implementation (explicit Search/Clear/Journal-select/Account-input bar) to the same server-rendered-once + `FB.list` pattern as `buildVoucherRegister`. Added a **Journal** column. Renamed **Reference → Doc No** on this report and, where the same journal-entry-reference concept applies, in `bill-detail.js`, `inbox.js` (×2) — `payables.js`'s `vendor_ref` "Reference" column is a different concept (the vendor's own invoice number) and was deliberately left alone. **Atomic Doc-No grouping** uses `tree: true` — a batch's lines are its tree: the first line (by account_code) is the parent and shows Doc No, the rest are children with Doc No blank; fb-list.js's own tree-filter/tree-sort doctrine ("children follow their parent") means a batch's lines can never be split apart by any column filter or sort, satisfying "atomic" natively — with `isFolded` overridden to always return false so every line stays visible (no fold/unfold; folding by default would defeat a line-level browsing report). Native FB.list column-header filter/sort (click-to-sort, `≡` filter dropdowns) replaces the removed Search/Clear bar entirely. **Known tradeoff, not resolved here:** tree-mode filters only evaluate parent-row fields — filtering by Account/Debit/Credit effectively filters by the batch's *first* line's value for that field, not any line in the batch; a search for an account that only appears on a non-first line of a batch won't surface it. This is the direct, and by the request's own framing intended, consequence of "atomic under filtering" — flagged in case it's not what was meant. Also fixed **two more stale `/books` links found while working in this file** (`buildCF`'s and P&L/BS drill-through account links, `reports/render.js:106,186,208,830,844` — missed by the earlier route-rename sweep) and **deleted another dead page** (`api/src/pages/bank-reconcile.js` — imported in `reports.js` but never routed, same pattern as the two dead pages found and deleted earlier in this spec's work). Live-verified: tree grouping renders correctly (parent row's Doc No cell populated, child rows' blank), no bespoke filter bar, zero page/console errors, no host-container overflow mismatch. Full API suite re-run clean: 211/211. |
| 2026-08-30 | **Fourth resize bug, root-caused properly this time.** "Journal → Line items still cut off" persisted after the earlier auto-resize fix because that fix measured height once, on the iframe's `load` event — but these reports populate their table via their own async script (`journal.list`/`bill.list`/`bill.aging` calls) that resolves *after* `load` fires, so the one measurement captured the still-empty shell's height. Fixed in both `renderIframe` (reports-hub.js) and `loadReportIframe` (payables.js): replaced the one-shot measurement with a `ResizeObserver` on the iframe's `contentWindow.document.body`, which re-measures every time the content's actual size changes — correct regardless of when the async population finishes. |
| 2026-08-30 | **Atomic grouping redesigned per correction — the tree-mode implementation above was wrong on two counts.** Magnus: (1) Line Items "should never be folded, should not support fold/unfold" — tree mode with `isFolded` forced false still carries a latent fold affordance in the framework even when suppressed; (2) "atomic" meant a filter must show *all* of a transaction's lines even when the filter only matches one of them — tree mode's "children follow their parent" only works in the parent's favor (a child's own match doesn't rescue the group), which isn't what was asked. Checked `fb-list.js` directly: filter (free-text and per-column `≡`) and sort all read a row's own field value with no hook to substitute a sibling's — true "any row matches → whole group shown, via native column-click filters" isn't achievable from report-side code alone. **Asked magnus before touching the shared file** (`fb-list.js` is used by every FB.list page in the app) — approved extending it. Shipped a new opt-in `groupKey` option (flat lists only, `cfg.tree` unaffected, zero behavior change for every other consumer that doesn't set it): filtering expands a passing row's match to its whole group; sorting keeps a group's rows contiguous, block position decided by the group's first row (the flat-list analog of the existing tree-mode `applyViewSortTree`, added as `applyViewSortGrouped`). `buildJournal` converted from `tree:true` to a plain flat list with `groupKey: 'batch_id'` — no `children()`/`isFolded` override needed at all now, simpler than the tree version. Live-verified the actual target case: filtering the Account column to a value that exists *only* on a transaction's second line still returns both lines, including the first line's Doc No — the exact scenario described. No fold-affordance element found anywhere in the DOM. Sort-by-Debit re-checked (batches stay contiguous). Full report tour + API suite re-run clean: 211/211. |
| 2026-08-30 | **Two follow-up refinements.** (1) Tab order: `report-registry.js`'s REPORT_REGISTRY array reordered so **Line items sits immediately after Transactions** (was last, after Trial Balance/General Ledger) — `reports-hub.js`'s tab strip is driven directly by array order, so this alone moved it; live-verified new order `Transactions, Line items, Trial Balance, General Ledger`. (2) Journal and Date columns now follow the exact same "first row of the atomic unit only" rule as Doc No — blank on every row past a batch's first line, and (matching a gap the first pass missed) explicitly rendered as truly empty rather than fb-list.js's default "—" empty-cell placeholder, via the same `display: v ? esc(v) : ''` pattern already used for Doc No. Confirmed safe under sort: `applyViewSortGrouped` only reorders which *group* comes first, never the row order *within* a group, so the blanked-except-first-row values stay correctly attached to the true first row regardless of which column is sorted. Live-verified and full API suite re-run clean: 211/211. |
| 2026-08-30 | **Column layout polish across Journal/Trial Balance/General Ledger.** Line items: Description moved to the far right, Date and Journal swapped — final order `Date, Journal, Doc No, Account, Account Name, Debit, Credit, Description` (both the FB.list `columns:` array and the static `<thead>` markup updated together; they're two separate things that must stay in sync — fb-list.js positions cells by `columns:` order, the hand-written `<thead>` just has to visually match). Trial Balance: Type column moved to the far left (`Type, Code, Account, Debit, Credit, Net`, was `Code, Account, Type, ...`). **Zero Debit/Credit cells now render blank instead of "0.00"** in Trial Balance and General Ledger — a new `fmtDC()` helper (`reports/render.js`, alongside the existing `fmt()`) applied only to Debit/Credit cells in `buildTB`/`buildGL`; deliberately *not* a change to `fmt()` itself, which stays as-is for every other numeric column (PL/BS/CF, TB's own Net column, GL's running Balance) where a genuine zero should still display as a number — confirmed live: TB's TOTAL row's Net column and GL's running-balance column still correctly show `0.00` where the balance really is zero, only Debit/Credit cells blank. (Journal Line Listing's Debit/Credit already blanked zeros via its existing `amtDisplay` — untouched, already correct.) Live-verified all four changes; full API suite re-run clean: 211/211. |
| 2026-08-30 | **General Ledger migrated to FB.list, magnus's own idea from a "talk, don't act" design discussion.** Same `groupKey: 'account_code'` mechanism as Journal Line Listing (each account's Opening Balance → transactions → Closing Balance is one atomic group). Replaces the bespoke Account-code search box with native column-header filtering (filtering the Account column keeps a matched account's whole group — Opening through Closing — together, same effective behavior as the old box, just via fb-list's `≡` dropdown) and adds persistent/sticky column headers. Running balance stays entirely server-computed and sequential-by-date, unaffected by client-side filtering — filtering only hides/shows groups, never recomputes anything. The account section-header row (bold, spans the table) has no fb-list equivalent — folded into the Opening Balance row's own Description-column display instead ("1680 — Fordringar hos kreditinstitut"), since fb-list has no separate "group header" row type. Date/Debit/Credit/Balance are deliberately not `sortable` — this report's Balance column is only meaningful in date order, and fb-list's groupKey sort reorders which group comes first, not rows within a group, so making these look sortable would invite clicks that do nothing useful. `gl` joined `IFRAME_REPORTS` in `reports-hub.js` (now needs `FB.list.create()` to run, same reasoning as the other three). `onChrome` (fires after every fb-list render, not just initial load — confirmed against `fb-list.js`'s own `syncChrome()` call sites) applies the opening/closing/txn row style classes, since fb-list has no per-row class hook; a one-shot post-`.load()` approach would have lost the styling after the first filter or sort. **Sticky headers needed two rounds to actually stick**, both real specificity conflicts, not authoring mistakes caught by a linter: (1) GL's own copy of the shared `th.fb-th-filterable { position: relative }` boilerplate outweighed a bare `th { position: sticky }` rule by CSS specificity regardless of source order; (2) after removing that local copy, `common.css`'s own global `th.fb-th-filterable { position: relative }` — loaded on every report via `<link>` — still won at equal specificity to a same-named rule added only inside GL's own later `<style>` block. Fixed by re-declaring `th.fb-th-filterable { position: sticky }` explicitly in GL's stylesheet, matching common.css's selector so cascade order (GL's `<style>` comes after the `<link>`) decides it, rather than fighting specificity. Live-verified: computed `th` position is `sticky`; old filter box gone; Account-column filter keeps a full 3-row group (Opening/txn/Closing) together and correctly excludes non-matching accounts; the existing TB→GL `?account=` drill-through link still pre-filters server-side, now resolving through the renamed `/journal` route. Full tab tour, Statements, and Payables unaffected; full API suite re-run clean: 211/211. |
| 2026-08-30 | **GL rendering slowly with up to three visible scrollbars — a resize feedback loop, self-inflicted by the previous entry.** `renderIframe`'s `ResizeObserver` (reports-hub.js) auto-grows the outer `<iframe>` to match its own content's height — but GL's own CSS sizes `.table-wrap` off `max-height: calc(100vh - 220px)`, and `100vh` *inside* an iframe means that iframe's own height. Resizing the iframe therefore changes its internal `100vh`, which changes `.table-wrap`'s max-height, which changes `document.body.scrollHeight`, which fires the ResizeObserver again — an unbounded resize/layout loop, live-confirmed as the cause of both the slowness (repeated reflow each cycle) and the multiple scrollbars (different in-progress states of the loop each holding their own active overflow container). GL is the only iframe report with an internal scrolling container (needed for its sticky headers), so it's also the only one that can hit this — voucher-register and Line items have no such container and were unaffected. **Fixed:** GL now gets a **fixed**-height iframe (`calc(100vh - 220px)`, matching what `.table-wrap`'s own sizing already assumed) with no ResizeObserver at all, instead of the auto-grow treatment — its own internal scroll is exactly what sticky headers need anyway, so this isn't a workaround, it's the design the auto-grow approach was never compatible with in the first place. voucher-register/Line items keep auto-grow, confirmed still returning plain pixel heights (unaffected). Live-verified: GL settles in ~440ms (was hanging/slow), single legitimate scroll context, zero page errors. Full API suite re-run clean: 211/211. |
| 2026-08-30 | **The resize loop was gone but two more scrollbars remained — a real structural issue, not a leftover of the fix.** With the resize loop fixed, `.table-wrap`'s sizing (`max-height: calc(100vh - 220px)`, a hand-guessed magic number for "viewport minus header/footer") could still be wrong, letting the report's own `body`/`html` overflow the fixed iframe height — the browser default-scrolls `html`/`body` when their content overflows, so that was a second scrollbar (the iframe's own document) stacked on top of `.table-wrap`'s. And the fixed iframe height itself (`calc(100vh - 220px)`, viewport-relative) was routinely taller than what was actually left after the page's own chrome (topbar, tabs, controls row) — the iframe then overflowed its OWN parent, forcing that parent's `overflow:auto` to scroll too. That's the third. **Fixed both, structurally rather than by re-guessing numbers:** (1) `buildGL`'s CSS restructured to a flex column — `.page` fills the iframe's full height, `.header`/`.footer` take their natural size, `.table-wrap` gets `flex:1; min-height:0` (exactly what's left) — making it structurally impossible for `body`/`html` to overflow the iframe height, no matter how tall the header/footer actually render. (2) The fixed iframe height itself is now computed in JS from the *actual* available space (`renderIframe` measures `#report-body`'s parent's real `clientHeight`, minus its known 1rem padding) rather than a `calc(100vh - Npx)` guess, so the iframe never overflows its own container. Live-verified at a realistic constrained viewport: exactly one scroll container active (`.table-wrap`) — the outer page and the iframe's own `body`/`html` both report `scrollHeight === clientHeight` (no scroll needed) in both the fits-on-screen case and the genuinely-taller-than-viewport case. Full tab tour and API suite re-run clean: 211/211. |
| 2026-08-30 | **§6 drafted** (report chrome cleanup: header, unified topbar download icon, CSV correctness) from a "talk, don't act" design discussion. Header stays in the HTML for PDF's benefit, hidden on screen only. Download icon moves into `common.js`'s existing topbar icon row, always present; SIE always available when jurisdiction permits (company+period scoped, not tab-scoped), CSV/PDF conditional on the active tab having exportable content — via a deliberately small `window.__fbDownloadCsv`/`window.__fbDownloadPdfUrl` per-page contract, not a formal register/subscribe API. CSV switches from DOM-scraping to reading each report's own row-data array for the three iframe-based reports specifically (voucher-register/Line items/GL — the only ones with both the cross-iframe DOM problem and the atomic-grouping blank-value problem); the seven fragment-rendered reports keep DOM-scraping, unaffected. No fill/blank setting — CSV always unblanked, PDF stays blanked like the screen. SIE type scope confirmed unchanged (type 4 only; 1–3 are strict subsets, no consumer needs them). Status: PROPOSED, not yet built. |
| 2026-08-30 | **§6 implemented.** Headers hidden screen-only via `@media print` on all four iframe reports (§6.2, resolving open question 1). Topbar icon added to `common.js`'s `navBar()`; `/api/:company/sie-status` (reports.js) exposes the existing `isSieExportEnabled` check for the icon's client-side gating; `updateDownloadHooks()` call sites added to `reports-hub.js`, `payables.js`, `accounting.js`, setting/clearing the two hooks on every tab switch, cleared again on soft-nav (`common.js`'s `fbNavigate`) so a stale hook from the previous page can't survive a client-side route change. CSV reads `JL_ROWS`/`GL_ROWS`/`VR_ROWS` via same-origin `frame.contentWindow` access (§6.3 open question 2, resolved as recommended) for the three iframe reports, generic DOM-scrape fallback for the rest; SIE via a new `_dlExportSie()` reusing the existing `/report?type=sie` URL pattern. Dropdown copy: "🖨 Print / PDF" / "⬇ CSV" / "⬇ SIE" (open question 3, resolved). Full tour live-verified (all 7 report tabs, Payables Aging/Control, Accounting Integrity, Bills tab correctly SIE-only) and API suite 211/211. **Two real bugs found and fixed during live verification, both distinct from anything the spec anticipated:** (1) *Download button did nothing on click, and the SIE menu item never appeared on any page.* Root cause: `fb-core.js` loads via `<script src>` in `<head>`, before `navBar()`'s topbar markup exists in `<body>` — the existing notifications-bell and `+`-menu wiring already accounts for this (deferred via `if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fn); else fn();`, with an explicit comment saying so), but the newly-added download-button wiring and the new `_initSieEnabled()` IIFE (fetching `/api/:company/sie-status`) were both written as unguarded top-level code — `document.getElementById('tb-dl-btn')` and `document.getElementById('app-shell')` silently returned `null` at that point, so the click listener was never attached and the SIE-status fetch was never even made. Fixed by wrapping both in the same deferred pattern already established in this file. (2) *Line items CSV still showed blanked Journal/Date/Doc No on continuation rows* — the exact defect §6.4 was written to prevent. Root cause: the atomic-grouping blank-past-first-row values (added in an earlier round, this table's entry above) were baked directly into `JL_ROWS` itself server-side (`buildJournal`, `reports/render.js`: `isFirst ? value : ''`), not applied at display time — so CSV, reading `JL_ROWS` directly per §6.4's own design, inherited the blanks. This was also a latent on-screen bug independent of CSV: `groupKey`'s sort keeps a batch's rows contiguous but doesn't guarantee the row carrying the non-blank value renders first within its group after a re-sort, so sorting by e.g. Account Code could in principle have shown a batch with no visible Journal/Date/Doc No at all. Fixed at the root: `rowsData` now carries the full value on every row, unconditionally; a new `jlBlankRepeats()` function, called from `onChrome` (fb-list.js's per-render hook, so it re-applies after every sort/filter, not just initial load), walks the rendered `#jl-body` rows in current DOM order and blanks the three cells' `innerHTML` only where the current row's `batch_id` matches the immediately preceding rendered row's — a screen-only, render-order-correct version of the same visual effect, with the underlying data never touched. Live-verified: CSV now shows full values on every row; on-screen display still blanks correctly; blanking survives a live column re-sort (checked by sorting Line items by Account and confirming exactly one filled row per contiguous group, not zero or duplicated). Full API suite re-run clean: 211/211 after each fix. |
